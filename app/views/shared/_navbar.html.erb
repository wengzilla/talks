  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="<%= root_path %>">
          <strong>TalkShare</strong>
        </a>
        <div class="nav-collapse collapse">
          <ul class="nav pull-right">
            <li><a href="<%= root_path %>">HOME</a></li>
            <li><a href="<%= talks_path %>">TALKS</a></li>
            <li><a href="<%= contact_path %>">CONTACT US</a></li>
            <% if current_user %>
              <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete, :class => "btn-header" %></li>
            <% else %>
              <li><%= link_to "Sign up", new_user_registration_path, :class => "btn-header" %></li>
              <li><%= link_to "Sign in", new_user_session_path, :class => "btn-header" %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>